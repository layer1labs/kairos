name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  build:
    name: Build & Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache Cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Check format
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy -- -D warnings -A non_snake_case -A unused_variables -A unused_imports

      - name: Build
        run: cargo build --verbose

      - name: Test
        run: cargo test --verbose

  governance:
    name: Governance Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install specsmith
        run: pip install specsmith

      - name: Run audit
        run: python -m specsmith audit --project-dir . || true
        # Audit is advisory for kairos — specsmith governs from the sister repo.

      - name: Check machine state sync (REQ-003)
        run: python -m specsmith sync --project-dir . || true
        # Sync is best-effort — kairos governance files may not have all docs.
