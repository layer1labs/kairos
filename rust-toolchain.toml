# Kairos — Rust toolchain pin
# Locks the project to the stable channel so CI and local builds are reproducible.
# Install Rust: https://rustup.rs/
# After install, run: rustup update stable
[toolchain]
channel = "stable"
