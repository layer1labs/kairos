#!/usr/bin/env bash
set -euo pipefail
PROJECT_ROOT="$(cd "$(dirname "$0")/.." && pwd)"

echo "kairos setup (Linux/macOS)"
# Non-Python project. Add project-specific setup steps here.
echo "NOTE: Edit scripts/setup.sh to add setup steps for cli-rust."
echo "Setup complete."
